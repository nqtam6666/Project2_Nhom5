@model Project2_Nhom5.Models.User

@{
    ViewData["Title"] = "Xóa Người dùng";
}

<div class="d-flex justify-content-between align-items-center mb-4">
    <div>
        <h2 class="mb-1">
            <i class="fas fa-trash me-2 text-danger"></i>Xóa Người dùng
        </h2>
        <p class="text-muted mb-0">Xác nhận xóa người dùng #@Model.UserId.ToString().PadLeft(6, '0')</p>
    </div>
    <a asp-action="Index" class="btn btn-outline-secondary">
        <i class="fas fa-arrow-left me-2"></i>Quay lại danh sách
    </a>
</div>

<div class="card-dashboard">
    <div class="card-body p-4">
        <div class="alert alert-danger" role="alert">
            <div class="d-flex align-items-center">
                <div class="bg-danger text-white icon-circle me-3">
                    <i class="fas fa-exclamation-triangle"></i>
                </div>
                <div>
                    <h5 class="alert-heading mb-1">Cảnh báo!</h5>
                    <p class="mb-0">Bạn có chắc chắn muốn xóa người dùng này? Hành động này không thể hoàn tác và sẽ xóa tất cả dữ liệu liên quan.</p>
                </div>
            </div>
        </div>
        
        <div class="row g-4">
            <div class="col-md-6">
                <div class="d-flex align-items-center mb-3">
                    <div class="bg-primary text-white icon-circle me-3">
                        <i class="fas fa-user"></i>
                    </div>
                    <div>
                        <h6 class="mb-1">Tên đăng nhập</h6>
                        <h4 class="text-primary mb-0">@Model.Username</h4>
                    </div>
                </div>
            </div>
            
            <div class="col-md-6">
                <div class="d-flex align-items-center mb-3">
                    <div class="bg-info text-white icon-circle me-3">
                        <i class="fas fa-envelope"></i>
                    </div>
                    <div>
                        <h6 class="mb-1">Email</h6>
                        <h4 class="text-info mb-0">@Model.Email</h4>
                    </div>
                </div>
            </div>
            
            <div class="col-md-6">
                <div class="d-flex align-items-center mb-3">
                    <div class="bg-success text-white icon-circle me-3">
                        <i class="fas fa-user-tag"></i>
                    </div>
                    <div>
                        <h6 class="mb-1">Vai trò</h6>
                        <h4 class="text-success mb-0">
                            @switch (Model.Role)
                            {
                                case "Admin":
                                    <span class="badge bg-danger">Quản trị viên</span>
                                    break;
                                case "Agency":
                                    <span class="badge bg-warning">Đại lý</span>
                                    break;
                                case "User":
                                    <span class="badge bg-primary">Người dùng</span>
                                    break;
                                case "Guest":
                                    <span class="badge bg-secondary">Khách</span>
                                    break;
                                default:
                                    <span class="badge bg-secondary">@Model.Role</span>
                                    break;
                            }
                        </h4>
                    </div>
                </div>
            </div>
            
            <div class="col-md-6">
                <div class="d-flex align-items-center mb-3">
                    <div class="bg-warning text-white icon-circle me-3">
                        <i class="fas fa-info-circle"></i>
                    </div>
                    <div>
                        <h6 class="mb-1">Trạng thái</h6>
                        <h4 class="text-warning mb-0">
                            @if (Model.Status == "active")
                            {
                                <span class="badge bg-success">Hoạt động</span>
                            }
                            else
                            {
                                <span class="badge bg-danger">Không hoạt động</span>
                            }
                        </h4>
                    </div>
                </div>
            </div>
        </div>
        
        @if (!string.IsNullOrEmpty(Model.Phone))
        {
            <hr class="my-4">
            <div class="row g-3">
                <div class="col-md-6">
                    <div class="d-flex align-items-center p-3 bg-light rounded">
                        <div class="bg-info text-white icon-circle me-3">
                            <i class="fas fa-phone"></i>
                        </div>
                        <div>
                            <small class="text-muted">Số điện thoại</small>
                            <div class="fw-bold">@Model.Phone</div>
                        </div>
                    </div>
                </div>
            </div>
        }
        
        @if (Model.Tickets.Any())
        {
            <hr class="my-4">
            <div class="alert alert-warning" role="alert">
                <div class="d-flex align-items-center">
                    <div class="bg-warning text-white icon-circle me-3">
                        <i class="fas fa-ticket-alt"></i>
                    </div>
                    <div>
                        <h6 class="alert-heading mb-1">Dữ liệu liên quan</h6>
                        <p class="mb-0">Người dùng này có @Model.Tickets.Count vé đã mua. Việc xóa sẽ ảnh hưởng đến dữ liệu vé.</p>
                    </div>
                </div>
            </div>
        }
        
        <hr class="my-4">
        <form asp-action="Delete" class="d-flex justify-content-end gap-2">
            <input type="hidden" asp-for="UserId" />
            <a asp-action="Index" class="btn btn-outline-secondary">
                <i class="fas fa-times me-2"></i>Hủy bỏ
            </a>
            <button type="submit" class="btn btn-danger">
                <i class="fas fa-trash me-2"></i>Xác nhận xóa
            </button>
        </form>
    </div>
</div>
