@model Project2_Nhom5.Models.Discount

@{
    ViewData["Title"] = "Xóa Khuyến mãi";
}

<div class="d-flex justify-content-between align-items-center mb-4">
    <div>
        <h2 class="mb-1">
            <i class="fas fa-trash me-2 text-danger"></i>Xóa Khuyến mãi
        </h2>
        <p class="text-muted mb-0">Xác nhận xóa khuyến mãi khỏi hệ thống</p>
    </div>
    <a asp-action="Index" class="btn btn-outline-secondary">
        <i class="fas fa-arrow-left me-2"></i>Quay lại danh sách
    </a>
</div>

<div class="row">
    <div class="col-md-4">
        <div class="card-dashboard border-danger">
            <div class="card-body text-center p-4">
                <div class="bg-danger text-white rounded-circle mx-auto mb-3" style="width: 80px; height: 80px; display: flex; align-items: center; justify-content: center;">
                    <i class="fas fa-exclamation-triangle fa-2x"></i>
                </div>
                <h4 class="text-danger mb-2">Cảnh báo!</h4>
                <p class="text-muted">Bạn sắp xóa khuyến mãi này khỏi hệ thống. Hành động này không thể hoàn tác.</p>
                
                <div class="bg-light rounded p-3 mb-3">
                    <i class="fas fa-percent fa-3x text-primary mb-2"></i>
                    <h5 class="mb-2">@Model.Code</h5>
                    @{
                        var discountTypeText = Model.DiscountType switch
                        {
                            "percent" => "Phần trăm",
                            "fixed" => "Số tiền cố định",
                            _ => Model.DiscountType
                        };
                    }
                    <span class="badge bg-info mb-2">@discountTypeText</span>
                    <span class="badge bg-secondary">@Model.Value</span>
                </div>
            </div>
        </div>
    </div>
    
    <div class="col-md-8">
        <div class="card-dashboard">
            <div class="card-body p-4">
                <h5 class="card-title mb-4">
                    <i class="fas fa-info-circle me-2 text-primary"></i>Thông tin khuyến mãi sẽ bị xóa
                </h5>
                
                <div class="alert alert-warning" role="alert">
                    <i class="fas fa-exclamation-triangle me-2"></i>
                    <strong>Lưu ý:</strong> Việc xóa khuyến mãi này sẽ ảnh hưởng đến các giao dịch đang sử dụng mã này.
                </div>
                
                <div class="row g-3">
                    <div class="col-md-6">
                        <div class="d-flex align-items-center p-3 bg-light rounded">
                            <div class="bg-primary text-white rounded-circle p-2 me-3">
                                <i class="fas fa-tag"></i>
                            </div>
                            <div>
                                <small class="text-muted">Mã khuyến mãi</small>
                                <div class="fw-bold">@Model.Code</div>
                            </div>
                        </div>
                    </div>
                    
                    <div class="col-md-6">
                        <div class="d-flex align-items-center p-3 bg-light rounded">
                            <div class="bg-info text-white rounded-circle p-2 me-3">
                                <i class="fas fa-percent"></i>
                            </div>
                            <div>
                                <small class="text-muted">Loại khuyến mãi</small>
                                <div class="fw-bold">@discountTypeText</div>
                            </div>
                        </div>
                    </div>
                    
                    <div class="col-md-6">
                        <div class="d-flex align-items-center p-3 bg-light rounded">
                            <div class="bg-success text-white rounded-circle p-2 me-3">
                                <i class="fas fa-dollar-sign"></i>
                            </div>
                            <div>
                                <small class="text-muted">Giá trị khuyến mãi</small>
                                <div class="fw-bold">
                                    @if (Model.DiscountType == "percent")
                                    {
                                        <div class="text-success fw-bold fs-5">@Model.Value%</div>
                                    }
                                    else if (Model.DiscountType == "fixed")
                                    {
                                        <div class="text-info fw-bold fs-5">@Model.Value.ToString("N0") VNĐ</div>
                                    }
                                </div>
                            </div>
                        </div>
                    </div>
                    
                    <div class="col-md-6">
                        <div class="d-flex align-items-center p-3 bg-light rounded">
                            <div class="bg-warning text-white rounded-circle p-2 me-3">
                                <i class="fas fa-calendar-times"></i>
                            </div>
                            <div>
                                <small class="text-muted">Ngày hết hạn</small>
                                <div class="fw-bold">@Model.ExpiryDate.ToString("dd/MM/yyyy")</div>
                            </div>
                        </div>
                    </div>
                </div>
                
                @if (!string.IsNullOrEmpty(Model.Description))
                {
                    <div class="mt-4">
                        <h6 class="mb-3">
                            <i class="fas fa-align-left me-2 text-primary"></i>Mô tả
                        </h6>
                        <div class="p-3 bg-light rounded">
                            @Model.Description
                        </div>
                    </div>
                }
                
                <form asp-action="Delete" class="mt-4">
                    <input type="hidden" asp-for="DiscountId" />
                    <div class="d-flex justify-content-end gap-2">
                        <a asp-action="Index" class="btn btn-outline-secondary">
                            <i class="fas fa-times me-2"></i>Hủy bỏ
                        </a>
                        <button type="submit" class="btn btn-danger">
                            <i class="fas fa-trash me-2"></i>Xác nhận xóa
                        </button>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>
